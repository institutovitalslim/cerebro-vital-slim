# Content Engine OS — template local sem segredos.
# Copie para .env e preencha as variáveis da stack pelo runtime governado.

# Hook Intelligence fica desativado até que ambos os valores sejam provisionados.
HOOK_INTELLIGENCE_ENABLED=false
HOOK_INTEGRATION_SECRET=
