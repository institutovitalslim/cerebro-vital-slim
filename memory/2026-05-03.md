- Nova regra canônica determinada por Tiaro: toda implementação operacional relevante no IVS deve terminar com implantação total na mesma execução, sem depender só de runtime e sem depender de autorização posterior para promoção ao cérebro.
- A governança central foi atualizada em `cerebro/empresa/skills/GOVERNANCA-SKILLS.md` com a regra de implantação total: runtime + camada canônica + atualização dos agentes impactados + registro durável via graphify.
- A regra também foi incorporada ao cérebro operacional do João em `JOAO-REGRAS-DE-OPERACAO.md`, para que liberação de ferramenta, capability ou integração relevante não fique apenas em configuração temporária.
- O princípio foi registrado com graphify no corpus `/root/cerebro-vital-slim/tmp/graphify-implantacao-total`, gerando `graphify-out/GRAPH_REPORT.md`, `graphify-out/graph.json` e `graphify-out/graph.html`.

- Browser/Lovable remediation advanced materially on 2026-05-03: enabled `agents.defaults.sandbox.browser.enabled = true` in `/root/.openclaw/openclaw.json`, but sandbox browser remained unavailable because the expected Docker image `openclaw-sandbox-browser:bookworm-slim` was not present; Docker itself was healthy.
- Confirmed the installed OpenClaw docs reference the sandbox browser image/provisioning path, but the cited local setup script was absent in the current install, so sandbox enablement in config alone does not provision runtime.
- Corrected strategy for VPS operation: do not depend on human `profile=user` browser sessions; prefer server-side host browser or sandbox browser on the VPS.
- Browser SSRF policy was tightened correctly by allowlisting only `lovable.dev` and `www.lovable.dev` in `/root/.openclaw/openclaw.json` under `browser.ssrfPolicy.allowedHostnames`, instead of opening broader private-network access.
- Manual Chrome on the VPS was verified to answer CDP successfully at `http://127.0.0.1:18800/json/version`, proving Chrome itself can expose DevTools when started manually.
- Host browser config was adjusted in `/root/.openclaw/openclaw.json` toward VPS operation with `browser.headless = true`, `browser.noSandbox = true`, explicit `browser.executablePath = /usr/bin/google-chrome`, and profile-level settings for `browser.profiles.openclaw.cdpPort = 18800`, `driver = "openclaw"`, `attachOnly = false`.
- Despite the above, the OpenClaw browser tool continued to fail with `Chrome CDP websocket for profile "openclaw" is not reachable after start`, while status still reported `running: false`, `cdpReady: false`, `cdpHttp: false`, and in at least one check exposed `executablePath: null` and `userDataDir: null` even after config fixes.
- The profile directory for `openclaw` was confirmed to exist materially with artifacts such as `Local State`, `Default/Preferences`, `SingletonLock`, `SingletonSocket`, and `SingletonCookie`, so the remaining browser blocker narrowed to OpenClaw’s internal browser runtime/launcher/config-resolution layer rather than missing Chrome, missing profile files, SSRF policy, or permissioning.
- Executive conclusion as of 2026-05-03: João’s Lovable/browser blocker is now localized to the OpenClaw host-browser runtime on the VPS; governance, SSRF host allowlisting, and Chrome presence are no longer the primary issue.
