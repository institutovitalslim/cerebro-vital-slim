# 2026-04-09

- Corrigi parcialmente a configuração de memória semântica do OpenClaw: troquei `agents.defaults.memorySearch.provider` de `gemini` para `openai` com fallback `local`, porque a chave do Gemini estava invalidada por vazamento. A busca semântica ainda não ficou 100% operacional, porque falta API key compatível para `openai` e o fallback local falha por ausência de `node-llama-cpp`.
- Reindexação manual de memória indicou estado relevante do projeto Clara/Z-API: bridge em pré-produção assistida, prompt da Clara desacoplado para arquivo externo, serviço systemd restaurado, Clara ativa para novos leads não pacientes, QA principal e mini-QA aprovados com risco residual de repetição/cadência.
- Concluída análise do perfil `@yuribarbosaoficial` para aprender vendas aplicáveis à Clara. Principais aprendizados: condução intencional, valor antes de preço, rapport breve e contextual, objeção tratada com clareza, follow-up mais elegante, evitar pressão/manipulação/urgência artificial.
- Concluída análise rápida do perfil `@oestevaosouza` por triangulação pragmática devido limitação pública do Instagram. Principais aprendizados: direção comercial, diagnóstico curto, foco em resultado/processo, microcompromissos claros, postura consultiva em vez de catálogo.
- Atualizei o prompt da Clara na bridge incorporando aprendizados de Yuri Barbosa + Oestevão Souza: mais direção consultiva, rapport breve e útil, microcompromissos, clareza de objeção, valor antes de preço sem enrolação, fechamento como consequência de clareza e segurança.
- Fiz uma segunda lapidação no prompt da Clara focada em naturalidade nas respostas de preço, objeção e fechamento, reduzindo tom defensivo e excesso de roteiro.
- Fiz uma terceira lapidação prática com dois blocos novos: `modo rápido para lead quente` e `respostas elegantes e curtas para dúvidas frequentes` (preço, convênio, está caro, quero pensar, já tentei de tudo).
- Rodei mini bateria prática de QA da nova lapidação comercial da Clara. Veredito: boa para uso real, principalmente em preço, objeções e condução de lead quente.
- Apliquei 3 ajustes finos pós-QA: resposta-padrão mais forte para disponibilidade, melhor manejo de urgência emocional sem prometer resultado, e bloco mais padronizado de condições comerciais / reserva / próximo passo.
- Fiz revisão final do prompt inteiro da Clara e cortei pequenas redundâncias para deixá-lo mais limpo e estável para produção.
- Pedido do Tiaro: atualizar o prompt da Clara na bridge, atualizar a memória da Clara e fazer commit no cérebro após consolidar esses aprendizados.
- Corrigi o acesso do `gog` para a conta `institutovitalslim@gmail.com` usando a senha do keyring recuperada via 1Password (`gog-keyring-pass` no vault `openclaw`), após falha `aes.KeyUnwrap(): integrity check failed`.
- Consegui abrir a planilha `ZAPI_Historico_Pacientes` (ID `1QXvRhElCx1t7mxMAwGkcvh5V7YyKLjP9zozSGH7LHnM`) e confirmei as abas `Folha1`, `contexto_paciente` e `pacientes`.
- Fiz triagem conservadora dos últimos 3 dias para identificar não pacientes/leads recentes com base na planilha histórica.
- Enviei follow-up com foco em agendamento para 4 leads recentes e não ambíguos via Z-API: Érica (`557185242700`), Edla Silva (`557196309675`), Ângela Macedo (`557181270613`) e Maria (`557199758320`).
- Mantive fora do disparo contatos de teste, internos, spam/ambíguos e nomes com forte indício de paciente/acompanhamento.
- Correção operacional importante: eu estava assumindo incerteza excessiva sobre o envio WhatsApp/Z-API a partir do contexto Telegram. Isso estava errado. O fluxo real existe e funciona via bridge/Z-API no host, inclusive com env em `/root/.openclaw/workspace/ops/zapi_bridge/zapi_bridge.env` e envio por `send-text`. Já houve envios reais bem-sucedidos. Daqui para frente, não devo responder como se o disparo estivesse "não validado" quando o ambiente já permitir envio operacional.
- Regra nova para follow-up comercial: antes de qualquer disparo, conferir a lista/base de pacientes da bridge e analisar o contexto completo da conversa até o ponto em que ela travou; o follow-up deve partir exatamente dali para inferir a objeção provável e contorná-la com contexto, nunca com mensagem genérica.
- Erro operacional importante: houve resposta indevida a uma paciente, Joyce (`+55 71 99954-3781`). Joyce deve ficar explicitamente fora de qualquer fluxo comercial/WhatsApp da Clara. Regra reforçada: paciente identificado = não responder comercialmente.
- Regra de robustez para troca de LLM: quando houver troca de modelo/provedor, nunca assumir que serviços, skills e crons herdam a configuração certa. Sempre auditar explicitamente overrides de modelo em `.env`, systemd user services, scripts de bridge e jobs agendados antes de considerar a migração concluída.
- Fonte de verdade para serviços operacionais: preferir `openai/gpt-5.4` como override explícito quando o fluxo depender do gateway local já validado; evitar deixar serviços críticos em `anthropic/claude-sonnet-4-6` sem validar cota/uso disponível.
- Caso concreto aprendido: a bridge Z-API da Clara parou de responder pacientes porque `zapi_bridge.env` estava com `OPENCLAW_MODEL_OVERRIDE=anthropic/claude-sonnet-4-6`, gerando erro de limite de uso do Claude. Corrigido para `OPENCLAW_MODEL_OVERRIDE=openai/gpt-5.4` e serviço reiniciado.
- Checklist obrigatório para qualquer troca de LLM em produção: (1) localizar overrides de modelo em `.env`, `x-openclaw-model`, scripts e services; (2) alinhar tudo ao modelo aprovado; (3) reiniciar serviços afetados; (4) validar um teste ponta a ponta; (5) registrar a mudança na memória para evitar regressão.
- Regra adicional: skills e crons que dependem de IA devem ter modelo explicitado ou validado depois de qualquer mudança global; nunca confiar só no default do agente para fluxos críticos.
- Criei `/root/cerebro-vital-slim/OPERATING_RULES.md` como fonte de verdade operacional para reduzir dois erros recorrentes: parar no meio da execução e falhar em contexto/memória/skills.
- Atualizei `AGENTS.md` com disciplina de execução: quando o usuário disser "pode seguir" ou equivalente, tratar como executar até conclusão e só voltar com concluído, bloqueio real, decisão necessária ou parada por segurança.
- Criei `/root/.openclaw/workspace/ops/zapi_bridge/FOLLOWUP_CHECKLIST.md` com checklist obrigatório antes de follow-up comercial: exclusão → paciente → contexto → ponto de trava → objeção provável → estratégia → mensagem.
- Criei `/root/cerebro-vital-slim/EXECUTION_CHECKLIST.md` como checklist geral para tarefas longas/críticas, definindo objetivo, critério de conclusão, estados válidos de retorno e proibindo updates vazios de intenção.
- Atualizei `OPERATING_RULES.md` para amarrar a regra de execução ao `EXECUTION_CHECKLIST.md`.
- Criei `/root/cerebro-vital-slim/CONTEXT_CANON.md` como mapa canônico de fontes de verdade por domínio (execução, bridge, LLMs, skills críticas) com regra explícita de precedência.
- Atualizei `AGENTS.md` para incluir `OPERATING_RULES.md` e `CONTEXT_CANON.md` no startup obrigatório de sessão.
- Atualizei `OPERATING_RULES.md` com uma seção de resolução de contexto: quando houver dúvida sobre contexto, skill ou regra, consultar primeiro o `CONTEXT_CANON.md` e seguir a ordem de precedência antes de agir.
- Criei `/root/cerebro-vital-slim/PREFLIGHT.md` como pré-checagem obrigatória para tarefas críticas, operacionais ou com chance de erro de contexto.
- Atualizei `AGENTS.md` para incluir `PREFLIGHT.md` no startup obrigatório de sessão.
- Atualizei `OPERATING_RULES.md` para exigir consulta ao `PREFLIGHT.md` antes de tarefas críticas ou com ação externa.
- Criei o preflight automático em script: `/root/.openclaw/workspace/ops/preflight/preflight_check.py`.
- O script já valida modos `generic` e `bridge-followup`, checando existência das fontes de verdade, checklist da bridge, exclusões e status da auditoria de LLM (`forbidden=0`).
- Gerei relatórios iniciais em `/root/.openclaw/workspace/ops/preflight/last_generic_check.json` e `/root/.openclaw/workspace/ops/preflight/last_bridge_followup_check.json`, ambos aprovados (`ok: true`).
- Integrei o preflight automático ao fluxo da bridge: `zapi_clara_bridge.py` agora roda `preflight_check.py bridge-followup` antes de qualquer resposta comercial. Se o preflight falhar, a Clara é bloqueada com motivo `preflight:*` em vez de responder no escuro.
- Reiniciei `zapi-clara-bridge.service` após integrar o preflight ao fluxo operacional.
- Correção da regra de destino dos boletos do Omie: o destino principal no Google Drive é `## Medical Contabilidade / #Instituto Vital Slim / Boletos de Programa de Acompanhamento / [NOME DO PACIENTE]`, com ID da pasta `1hbF8K-wil6NNyQ2PyXZK8PZ1jEZhccOr`, na conta `medicalemagrecimento@gmail.com`. Não existe pasta intermediária `Pacientes` para boletos; as subpastas dos pacientes ficam diretamente dentro de `Boletos de Programa de Acompanhamento`. Sempre verificar se a pasta do paciente já existe antes de criar e nunca duplicar pastas. O caminho local sincronizado permanece `C:\Users\tiaro\Documents\## Medical Contabilidade\#Instituto Vital Slim\Boletos de Programa de Acompanhamento\[Nome do Paciente]`. Exemplo de nomenclatura: `Boleto_P001-009_R$1115_Venc07-04-2026.pdf`.
- Script operacional dos boletos: `/root/.openclaw/workspace/skills/omie-boletos/scripts/omie_boletos.py`; `--local` salva no disco local, `--drive` salva no Google Drive, `--listar` apenas lista sem baixar, e o padrão sem flag é `--drive`.
- Credenciais necessárias para boletos Omie: `OMIE_APP_KEY` e `OMIE_APP_SECRET` de `/root/.openclaw/secure/omie_api.env`, `GOG_KEYRING_PASSWORD` vindo do 1Password para acesso ao Drive, conta Drive `medicalemagrecimento@gmail.com`.
- Skill criada: `agenda-diaria-whatsapp` (2026-04-07) para envio automático da agenda diária por WhatsApp via Z-API.
- Localização da skill `agenda-diaria-whatsapp`: `/root/.openclaw/workspace/skills/agenda-diaria-whatsapp/`.
- Cron da agenda diária por WhatsApp: ID `2ba465d4-3dd0-435b-bb12-1576ed6c0403`, horário 06:00 em `America/Sao_Paulo`, todos os dias.
- Destinatários corretos da agenda diária por WhatsApp: Tiaro `5571986968887`, Dra. Daniely `5571996962059`, Liane `5571991574827`.
- Regra operacional da agenda diária por WhatsApp: enviar SOMENTE via `curl`/`exec` para Z-API, nunca via canal whatsapp do OpenClaw.
- Parâmetros do Z-API para a agenda diária: instance `3CF367BB00EB205F87468A74AFBCE7F1`, token `C26CFC41175FD987513C3202`, client-token `F277815dcf4e94be7bc2861e8ae9fc369S`, número conectado `7138388708`.
- Fonte dos dados da agenda diária por WhatsApp: QuarkClinic API via script `quarkclinic_api.py`.
- Atualização da skill `tweet-carrossel` para v2.0 com regras obrigatórias novas.
- Regra crítica da skill `tweet-carrossel`: SEMPRE usar NanoBanana 2 (`google/gemini-3.1-flash-lite-preview`) para gerar imagens; NUNCA usar GPT ou DALL-E para imagens de carrossel; API key Gemini atualizada em `auth-profiles.json` (`google:manual`).
- Estrutura obrigatória do carrossel na skill `tweet-carrossel`: (1) CAPA com foto gerada por NanoBanana 2 + texto adicionado por Pillow usando Montserrat Black, branco + dourado `#9F8844`; (2) SLIDE 2 em formato tweet + screenshot PubMed do paper quando houver pesquisa; (3) SLIDES 3+ em formato tweet puro via script `make_tweet_slides.py`.
- Regras visuais obrigatórias para a Dra. Daniely na capa: semblante sério (nunca sorrindo), blazer escuro (nunca jaleco), braços cruzados, fundo de laboratório com frascos, cápsulas douradas no canto superior direito.
- Regras dos slides tweet na skill `tweet-carrossel`: texto em cor única `#c8c8c8`, mesmo tamanho, sem destaques; texto curto sem imagem deve ficar centralizado verticalmente; nunca deixar texto colado no topo com área vazia embaixo; colocar gancho no final de cada slide; CTA no último slide com botão dourado.
- Atualização crítica da skill `tweet-carrossel` para v3.0: fluxo obrigatório passa a ser primeiro criar a COPY (texto), obter aprovação do usuário, e só depois gerar imagens; nunca gerar imagens antes da copy aprovada.
- A skill `tweet-carrossel` agora tem dois tipos obrigatórios de slide tweet: TIPO A (com foto) = texto no topo + foto na parte inferior ocupando ~40-45% do slide com border-radius 12px; TIPO B (sem foto) = todo o bloco centralizado verticalmente com respiro.
- Regras visuais adicionais da skill `tweet-carrossel` v3.0: sem ícone de mute, sem contador de slide; specs exatas de layout: avatar 72px, nome 32px bold branco, handle 20px cinza `#71767B`, corpo 38px branco `#c8c8c8`, gap entre parágrafos 40px, margens 48-64px.
- Regra reforçada da skill `tweet-carrossel` v3.0: sempre NanoBanana 2 para imagens, nunca GPT/DALL-E.
- Nova regra crítica no fluxo da skill `tweet-carrossel`: a etapa 1 agora tem 7 passos obrigatórios: (1) usuário envia tema, (2) Clara cria rascunho da copy, (3) Clara aciona a skill `llm-council` para avaliar e refinar a copy, (4) Clara incorpora o feedback do conselho e gera a copy final, (5) Clara apresenta a copy final para aprovação, (6) usuário aprova ou ajusta, (7) só após aprovação gerar imagens.
- Regra mandatória da skill `tweet-carrossel`: a skill `llm-council` deve ser acionada antes de apresentar a copy ao usuário, para garantir qualidade máxima e reduzir retrabalho.