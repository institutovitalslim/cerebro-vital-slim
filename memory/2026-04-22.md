# 2026-04-22

## Diretriz canônica de honestidade

- Tiaro definiu uma regra explícita para a operação: sempre dizer a verdade, mesmo quando ela for desconfortável ou inconveniente.
- Não suavizar, não enviesar e não responder para agradar.
- Priorizar precisão, clareza e realidade acima de emoção, suposição ou conveniência.
- Quando houver incerteza real, dizer claramente que não sabe em vez de adivinhar.

## Imagens da Dra. com NanoBanana 2 Pro

- Novo comando canônico para prompts com foto real de referência da Dra.: `Enable strict facial consistency mode. Prioritize the facial features from the provided reference image for all subsequent generations. Maintain the subject's identity accurately while only adapting the pose, lighting, and background. Do not alter the core facial structure.`
- Essa cláusula deve ser usada como reforço de consistência facial, mas o resultado final continua exigindo validação visual.

## WhatsApp / ElevenLabs TTS

- Tiaro criou uma API key do ElevenLabs para que eu possa responder pacientes via áudio quando eles enviarem áudio pelo WhatsApp.
- Chave recuperada do 1Password (vault `openclaw`, item `Elevenlabs API`) e salva em `/root/.openclaw/secure/elevenlabs.env`.
- Bridge `zapi_clara_bridge.py` modificada para suportar respostas de áudio:
  - Detecta mensagens de áudio no webhook da Z-API
  - Baixa o áudio da URL fornecida
  - Transcreve via Whisper (OpenAI)
  - Envia a transcrição para a Clara
  - Converte a resposta para áudio via ElevenLabs TTS
  - Envia o áudio de volta via Z-API `/send-audio` com `audioBase64`
- Serviço `zapi-clara-bridge.service` reiniciado com as novas configurações.

## Buffer Social Media

- Tiaro criou API key do Buffer para postar nas redes sociais da Dra. via Buffer.
- Skill `buffer-social` criada em `~/.openclaw/workspace/skills/buffer-social/`.
- Script `post_buffer.py` implementado para criar posts via Buffer API.
- API key salva em `/root/.openclaw/secure/buffer.env`.
- Inicialmente bloqueado porque tentei usar a REST API, mas o token é OIDC para GraphQL.
- Corrigido: adaptado para usar a GraphQL API do Buffer (`https://api.buffer.com/`).
- Mutation `CreateIdea` testada e funcionando: criou post de teste com sucesso (`id: 69e9275415b2e6acbd361053`).

## Flush de compactação

- Confirmado como memória durável de hoje: a diretriz truth-first do Tiaro, a cláusula canônica de consistência facial para fotos da Dra. com NanoBanana 2 Pro, e a nova integração ElevenLabs TTS para respostas de áudio no WhatsApp.
