- Tiaro explicitou incômodo operacional central: quando a Clara já sabe fazer algo, ele odeia que ela responda como se não soubesse por não achar isso na memória operacional. Isso virou direcionador estrutural para criar/manter uma camada de memória operacional recuperável.
- Em 2026-04-30 foi consolidada a nova camada `cerebro/operacional/` com foco em recuperabilidade, incluindo `INDEX-PEDIDOS-RECORRENTES.md`, `CHECKLIST-RECUPERABILIDADE.md` e `PORTAS-UNICAS-DE-EXECUCAO.md`; essa camada deve ser continuamente alimentada com a linguagem real de pedido do Tiaro.
- Tiaro autorizou seguir com plano de manutenção contínua tanto para o cérebro quanto para a memória da Clara.
## IVS / Maria — 2026-04-30
- Regra canônica reforçada por Tiaro: toda vez que for armazenar algo na memória ou no cérebro, usar **graphify**; não registrar aprendizados estruturais por outro caminho.
- Incidente operacional da Clara no WhatsApp: lead inbound ficou sem resposta e Tiaro precisou assumir manualmente. Nova regra operacional definida pelo Tiaro: **SLA de primeira resposta = 1 minuto**; se a Clara não responder nesse prazo, **escalar imediatamente para Tiaro e Liane**.
- Incidente crítico adicional da Clara: repetição aleatória da mesma mensagem no WhatsApp. O pause lógico não bastou; o bloqueio efetivo só ocorreu após interromper o processo da bridge. Causa mais provável identificada: deduplicação frágil (dependente de `messageId` em memória), ausência de cooldown por telefone, processamento assíncrono suscetível a reentrega/reentrada de webhook.
- Diretriz de correção da bridge da Clara antes de religar: endurecer deduplicação persistente por telefone + `messageId` + hash do texto, bloquear repetição em janela curta, implementar cooldown por número, auto-pause/circuit breaker para resposta repetida, e só retomar após teste controlado.
- No grupo AI Vital Slim, o tópico `5782` teve o nome atualizado para **Marketing**; usar esse nome como referência corrente nas entregas e no contexto operacional.
- Tiaro validou que, para a apresentação do programa exibida na TV durante a consulta, o formato ideal não é slide deck e sim uma estrutura tipo landing page em seções, com progressão de convencimento consultivo.
- Foi definido um fluxo em que a Maria busca os resultados de exames do paciente e as respostas do formulário de pré-consulta e injeta esses dados na apresentação, tornando a peça híbrida: institucional + personalizada.
- Diretriz estratégica derivada: a apresentação deve operar em 3 camadas: (1) camada fixa institucional do IVS, (2) camada variável do paciente com nome/objetivos/dores/rotina/exames, e (3) camada de conexão explicando por que, no caso específico, o programa de acompanhamento faz sentido.
- Recomendação operacional associada: construir um template master parametrizável de HTML para essa apresentação, com placeholders como `paciente_nome`, `objetivo_principal`, `incômodo_central`, `rotina_dificultadora`, `ponto_positivo`, `exames_alerta[]`, `implicacoes[]`, `conexao_programa` e `cta_final`.
- Novo achado operacional na Clara após religamento: a bridge entrou sem contexto em conversa manual que Tiaro já estava conduzindo com uma lead. Causa identificada no código: ausência de trava real de handoff/manual takeover por número; a lógica atual de `should_respond_to_lead()` permite resposta para `existing_lead` e até `new_contact`, então qualquer inbound textual de lead conhecida pode reativar a Clara no meio da conversa humana.
- Conclusão durável: além da deduplicação e anti-loop, a Clara precisa de política de posse da conversa (ownership), com `manual_assume`/`manual_release` realmente bloqueantes, janela de silêncio/bloqueio após atendimento humano e critério restrito de entrada antes de qualquer retomada segura.
