# 2026-04-25

## Template de apresentação reconstruído com HTML aprovado do Erick

### Contexto
Tiaro aprovou o HTML da apresentação do Erick (dark theme, hero com foto da Dra., stats, diagnóstico, anchor, histórico, exames, timeline, contexto, CTA verde, referências) como o novo padrão da skill `geracao-apresentacao-paciente`.

### O que foi feito
- Template base da skill substituído pelo HTML aprovado do Erick
- Convertido para Jinja2 com placeholders dinâmicos:
  - `{{ nome_paciente }}`, `{{ idade_paciente }}`, `{{ crm_medico }}`
  - `{{ stats_section | safe }}`, `{{ diagnostico_section | safe }}`, `{{ anchor_section | safe }}`
  - `{{ history_section | safe }}`, `{{ exams_section | safe }}`, `{{ timeline_section | safe }}`
  - `{{ contexto_section | safe }}`, `{{ references_section | safe }}`
- Script `gerar_apresentacao.py` reescrito para usar Jinja2
- Seções dinâmicas geradas a partir dos dados dos questionários (histórico, contexto)
- Seções clínicas (stats, diagnóstico, exames) deixadas como placeholders para o agente preencher após análise dos PDFs

### Arquivos alterados
- `skills/geracao-apresentacao-paciente/assets/template-apresentacao.html` — novo template base
- `skills/geracao-apresentacao-paciente/scripts/gerar_apresentacao.py` — reescrito com Jinja2

### Pendente
- Trocar a logo no template para a versão oficial do brand kit (Tiaro pediu pra fazer depois)

---

## Pré-consulta do portal IVS corrigida

### Problema
Formulário preenchido em `preconsulta.institutovitalslim.com.br` (Tiaro, 2026-04-24 21:07) não gerou resumo no grupo AI Vital Slim.

### Diagnóstico
O código do portal (`/root/ivs-preconsulta/src/app/api/submit/route.ts`) **já tinha** a função `notifyTelegram()` que envia pro tópico 271. O problema era que o build do Next.js estava desatualizado — os logs do PM2 mostravam erro "Could not find a production build".

### Solução
- O PM2 reiniciou o processo automaticamente e o build foi refeito
- Portal confirmado funcionando em `localhost:3001`
- Último log: `[telegram] OK msg_id= 4572`
- Criado script fallback `notificar_resumos_portal_ivs.py` para reenvios manuais
- Removido cron do sistema (não precisa — portal já envia no submit)

### Arquivos criados
- `skills/geracao-apresentacao-paciente/scripts/notificar_resumos_portal_ivs.py`
- `skills/geracao-apresentacao-paciente/state/notified_portal_submissions.json`

---

## Commit
- Hash: `c11a5c88`
- Mensagem: feat: reconstrói template de apresentação com HTML aprovado do Erick + corrige pré-consulta

---
## 2026-04-25 15:10-16:22 — HomeMatch Club: Build estático finalizado e empacotado

### Contexto
- Solicitação do Tiaro: gerar o build estático do app e empacotar para deploy no cPanel.

### O que foi feito
1. Rodou `STATIC_EXPORT=1 npm run build` com sucesso.
2. Build gerado em `deliverables/homematch-app/out/`.
3. Empacotamento:
   - `deliverables/homematch-app/homematch-static-export.zip` (formato ideal para cPanel)
   - `deliverables/homematch-app/homematch-static-export.tar.gz` (fallback)
4. Arquivos no zip: `index.html`, `_next/`, páginas estáticas (`feed`, `dashboard`, `properties/...` etc.)

### Rotas geradas
Estáticas: `/`, `/admin`, `/auth`, `/chat`, `/dashboard`, `/feed`, `/match`, `/offer`, `/onboarding`, `/properties/new`, `/verification`
SSG: `/properties/aurora-park-101`, `/properties/costa-mar-22`, `/properties/verde-habitat-8`

### Estado de infra
- SSL: ok (Let's Encrypt válido para `homematch.institutovitalslim.com.br` e `www.homematch.institutovitalslim.com.br`)
- Subdomínio no cPanel: ok
- Build estático: ok
- Pacote para upload: ok

### Instruções dadas ao Tiaro
- Subir o `.zip` no Gerenciador de Arquivos do cPanel
- Extrair em `public_html/homematch` (raiz do subdomínio)
- Garantir que `index.html` fique direto na raiz, sem subpasta `out`

### Pendente
- Upload e extração no cPanel (aguardando Tiaro enviar print ou confirmar)
