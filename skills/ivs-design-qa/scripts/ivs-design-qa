#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
if [[ ! -d "$ROOT/node_modules/playwright-core" ]]; then
  npm ci --prefix "$ROOT" --ignore-scripts --no-audit --no-fund >/dev/null
fi
exec python3 "$ROOT/scripts/ivs_design_qa.py" "$@"
